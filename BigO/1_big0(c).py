def constant_algo(items):
    result = items[0] * items[0]
    print(items)
constant_algo([4, 5, 6, 8])

# Constant O(1)
# Logarithm O(logn)
# Linear O(n)
# Polynomial O(n^2), O(n^n)
# Exponential O(2^n)